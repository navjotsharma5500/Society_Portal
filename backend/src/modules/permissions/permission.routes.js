const express=require("express"),c=require("./permission.controller"),v=require("./permission.validation");const router=express.Router();router.get("/catalog/grouped",c.grouped);router.route("/").post(v.validateCreate,c.create).get(v.validateList,c.list);router.patch("/:permissionId/status",v.validateStatus,c.status);router.route("/:permissionId").get(c.get).patch(v.validateUpdate,c.update);module.exports=router;
