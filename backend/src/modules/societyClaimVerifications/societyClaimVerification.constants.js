const VERIFICATION_DECISIONS = Object.freeze({
  APPROVED: "APPROVED",
  REJECTED: "REJECTED",
  REQUEST_CHANGES: "REQUEST_CHANGES",
});
const PAGINATION = Object.freeze({
  DEFAULT_PAGE: 1,
  DEFAULT_LIMIT: 20,
  MAX_LIMIT: 100,
});
module.exports = { VERIFICATION_DECISIONS, PAGINATION };
