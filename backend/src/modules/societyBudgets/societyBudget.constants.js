const BUDGET_STATUSES = Object.freeze({ ACTIVE: "ACTIVE", INACTIVE: "INACTIVE", CLOSED: "CLOSED" });
const TRANSACTION_TYPES = Object.freeze({
  INITIAL_ALLOCATION: "INITIAL_ALLOCATION", ALLOCATION_INCREASE: "ALLOCATION_INCREASE",
  ALLOCATION_DECREASE: "ALLOCATION_DECREASE", MANUAL_ADJUSTMENT: "MANUAL_ADJUSTMENT",
  RESERVE: "RESERVE", RELEASE_RESERVE: "RELEASE_RESERVE", UTILIZE: "UTILIZE",
  REFUND: "REFUND", CLOSE: "CLOSE",
});
const DIRECTIONS = Object.freeze({ CREDIT: "CREDIT", DEBIT: "DEBIT" });
const ADJUSTMENT_TYPES = Object.freeze({ INCREASE: "INCREASE", DECREASE: "DECREASE" });
const ACADEMIC_SESSION_PATTERN = /^\d{4}-\d{2}$/;
const PAGINATION = Object.freeze({ DEFAULT_PAGE: 1, DEFAULT_LIMIT: 20, MAX_LIMIT: 100 });

module.exports = { BUDGET_STATUSES, TRANSACTION_TYPES, DIRECTIONS, ADJUSTMENT_TYPES, ACADEMIC_SESSION_PATTERN, PAGINATION };
