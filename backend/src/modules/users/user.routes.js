const express=require("express"),controller=require("./user.controller"),validation=require("./user.validation");const router=express.Router();router.get("/",validation.validateList,controller.list);router.patch("/:userId/status",validation.validateStatus,controller.status);router.patch("/:userId/login-access",validation.validateLogin,controller.login);router.get("/:userId",controller.get);module.exports=router;
