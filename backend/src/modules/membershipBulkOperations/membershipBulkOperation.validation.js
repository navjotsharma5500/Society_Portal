const multer=require("multer"),AppError=require("../../common/errors/AppError"),{END_REASONS}=require("../societyMemberships/societyMembership.constants");const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:10*1024*1024},fileFilter:(req,file,cb)=>cb(null,/\.xlsx$/i.test(file.originalname))}).single("file");const selected=(req,res,next)=>{try{if(!Array.isArray(req.body.membershipIds)||!req.body.membershipIds.length)throw new AppError("Membership IDs are required",400,"VALIDATION_ERROR");if(req.body.membershipIds.length>500)throw new AppError("Bulk operation is too large",413,"BULK_OPERATION_TOO_LARGE");if(!END_REASONS.includes(req.body.endReasonCode))throw new AppError("Invalid reason",400,"VALIDATION_ERROR");next()}catch(e){next(e)}};module.exports={upload,selected};
