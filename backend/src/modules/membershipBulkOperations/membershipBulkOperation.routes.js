const r=require("express").Router(),c=require("./membershipBulkOperation.controller"),v=require("./membershipBulkOperation.validation"),{authenticateSession}=require("../auth/auth.middleware");r.use(authenticateSession);r.post("/end-selected",v.selected,c.selected);r.post("/end-filter/preview",c.previewFilter);r.post("/:operationSessionId/confirm",c.confirm);r.get("/end-template",c.template);r.post("/excel/preview",v.upload,c.excelPreview);r.post("/excel/:operationSessionId/confirm",c.confirm);module.exports=r;
