const mongoose = require("mongoose"), { STATUSES, SCOPES } = require("./undertaking.constants"), { attachPublicId } = require("../publicIds/publicId.service");
const schema = new mongoose.Schema({ title: { type: String, required: true, trim: true, maxlength: 200 }, content: { type: String, required: true, maxlength: 50000 }, version: { type: String, required: true, trim: true, maxlength: 50 }, status: { type: String, enum: Object.values(STATUSES), default: STATUSES.DRAFT, index: true }, scope: { type: String, enum: Object.values(SCOPES), required: true, index: true }, isMandatory: { type: Boolean, default: true }, requireScrollToEnd: { type: Boolean, default: false }, checkboxLabel: { type: String, default: "I have read and agree to this undertaking.", maxlength: 500 }, effectiveFrom: Date, effectiveUntil: Date, createdBy: { type: mongoose.Schema.Types.ObjectId, ref: "User" }, updatedBy: { type: mongoose.Schema.Types.ObjectId, ref: "User" }, metadata: { type: mongoose.Schema.Types.Mixed, default: () => ({}) } }, { timestamps: true });
attachPublicId(schema, "UNDERTAKING");
module.exports = mongoose.model("Undertaking", schema);
