const LEADERSHIP_ROLES = Object.freeze({
  PRESIDENT: "PRESIDENT",
  VICE_PRESIDENT: "VICE_PRESIDENT",
});

const LEADERSHIP_STATUSES = Object.freeze({
  ACTIVE: "ACTIVE",
  INACTIVE: "INACTIVE",
  ENDED: "ENDED",
});

const PAGINATION = Object.freeze({
  DEFAULT_PAGE: 1,
  DEFAULT_LIMIT: 20,
  MAX_LIMIT: 100,
});

const ACADEMIC_SESSION_PATTERN = /^\d{4}-\d{2}$/;

module.exports = {
  LEADERSHIP_ROLES,
  LEADERSHIP_STATUSES,
  PAGINATION,
  ACADEMIC_SESSION_PATTERN,
};
