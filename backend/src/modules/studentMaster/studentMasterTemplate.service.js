const ExcelJS=require('exceljs')
const header=sheet=>{const row=sheet.getRow(1);row.font={bold:true,color:{argb:'FFFFFFFF'}};row.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF86172C'}};row.alignment={vertical:'middle'};row.height=24;sheet.autoFilter={from:'A1',to:row.getCell(row.cellCount).address}}
const studentColumns=[['Name*',26],['Email*',30],['Contact Number*',20],['Roll Number',18],['Course',18],['Branch',20],['Year',12],['CGPA',10],['Date of Birth',16],['Blood Group',14],['Hostel',16],['Room Type',16],['Room Number',16],['Father Name',24],['Father Email',30],['Father Contact',20],['Mother Name',24],['Mother Email',30],['Mother Contact',20],['Address Line 1',30],['Address Line 2',30],['City',18],['State',18],['Postal Code',16],['Country',18],['Login Allowed',16],['Record Status',18],['Profile Picture URL (Optional)',38]]
const createTemplate=async()=>{const wb=new ExcelJS.Workbook();wb.creator='TIET Society Portal';const students=wb.addWorksheet('Students',{views:[{state:'frozen',ySplit:1}]});students.columns=studentColumns.map(([columnHeader,width])=>({header:columnHeader,width}));header(students);students.getColumn(9).numFmt='yyyy-mm-dd';for(let row=2;row<=500;row++){students.getCell(row,26).dataValidation={type:'list',allowBlank:true,formulae:['"TRUE,FALSE"']};students.getCell(row,27).dataValidation={type:'list',allowBlank:true,formulae:['"ACTIVE,INACTIVE,ARCHIVED"']}}const refs=wb.addWorksheet('Society References',{views:[{state:'frozen',ySplit:1}]});refs.columns=[['Student Email*',30],['Society Code',18],['Society Name',30],['Imported Role Code',22],['Imported Role Name',24],['Current Role Reference',22],['Academic Session',20]].map(([columnHeader,width])=>({header:columnHeader,width}));header(refs);const instructions=wb.addWorksheet('Instructions');instructions.columns=[{width:30},{width:105}];instructions.addRows([['TIET Student Master Import','Complete Students and optional Society References sheets, preview, then confirm.'],['Mandatory fields','Only Name, Email and Contact Number are mandatory.'],['Unique identity','Student email must be unique. Roll number is optional but unique when supplied.'],['Date of Birth','Use an Excel date cell or YYYY-MM-DD. Example: 2004-01-26.'],['Society references','Use separate rows for multiple society references.'],['Role safety','Imported roles are verification references only and never become active portal roles automatically.'],['Import flow','Preview is mandatory before confirmation.'],['File limit','XLSX only, maximum 10 MB.']]);instructions.getRow(1).font={bold:true,size:14,color:{argb:'FF86172C'}};return wb.xlsx.writeBuffer()}
module.exports={createTemplate,studentColumns,header}
