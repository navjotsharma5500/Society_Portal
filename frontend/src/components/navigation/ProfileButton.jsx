import { useEffect, useState } from 'react'
import { useLocation, useNavigate } from 'react-router-dom'
import { useAuth } from '../../modules/auth/hooks/useAuth'
export default function ProfileButton(){const auth=useAuth(),location=useLocation(),navigate=useNavigate(),[imageFailed,setImageFailed]=useState(false),avatar=auth.user?.profilePictureUrl;useEffect(()=>setImageFailed(false),[avatar]);const target=auth.isStudent?'/student/profile':location.pathname.startsWith('/adosa')?'/adosa/profile':location.pathname.startsWith('/admin-workspace')?'/admin-workspace/profile':location.pathname.startsWith('/assistant')?'/assistant/profile':location.pathname.startsWith('/dosa-staff')?'/dosa-staff/profile':location.pathname.startsWith('/society-admin/')?`${location.pathname.split('/').slice(0,3).join('/')}/profile`:'/admin/profile';const initials=auth.user?.displayName?.split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()||'U';return <button className="avatar-button" aria-label="Open profile" onClick={()=>navigate(target)}>{avatar&&!imageFailed?<img src={avatar} alt="" onError={()=>setImageFailed(true)}/>:initials}</button>}
