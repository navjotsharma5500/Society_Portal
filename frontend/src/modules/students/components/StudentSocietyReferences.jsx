import {Card,StatusChip} from '../../../design-system'
export default function StudentSocietyReferences({references=[]}){return <Card title="Imported Society References" description="Reference only — this does not assign portal permissions.">{references.length?<div className="student-reference-list">{references.map((ref,index)=><article key={ref._id||index}><div><b>{ref.societyName||ref.societyCode||'Unresolved society'}</b><StatusChip status={ref.isCurrent?'Active':'Inactive'}/></div><p>{ref.importedRoleName||ref.importedRoleCode||'Role not specified'}</p><small>{ref.academicSession||'Academic session not specified'}</small></article>)}</div>:<p className="muted">No imported society references.</p>}</Card>}
