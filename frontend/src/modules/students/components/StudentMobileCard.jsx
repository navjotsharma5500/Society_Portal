import {Avatar,Button,Card,StatusChip} from '../../../design-system'
import {resolveProfilePicture} from '../../../utils/profilePicture'
export default function StudentMobileCard({student,onView,onEdit,onLogin,onStatus}){return <Card><div className="student-mobile-head"><Avatar name={student.name} src={resolveProfilePicture(null,student)}/><div><b>{student.name}</b><small>{student.email}</small></div><StatusChip status={student.recordStatus}/></div><div className="student-mobile-meta"><span>Roll <b>{student.rollNumber||'—'}</b></span><span>Course <b>{[student.course,student.branch].filter(Boolean).join(' / ')||'—'}</b></span><span>Login <b>{student.isLoginAllowed?'Allowed':'Disabled'}</b></span></div><div className="student-mobile-actions"><Button variant="ghost" onClick={()=>onView(student)}>View</Button><Button variant="ghost" onClick={()=>onEdit(student)}>Edit</Button><Button variant="ghost" onClick={()=>onLogin(student)}>{student.isLoginAllowed?'Disable':'Enable'}</Button><Button variant="ghost" onClick={()=>onStatus(student)}>Status</Button></div></Card>}
