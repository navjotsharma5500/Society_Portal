const today=()=>new Date().toISOString().slice(0,10)
export function validateEvent(event){const errors={};if(!event.eventName?.trim())errors.eventName='Event name is required.';if(!event.startDate)errors.startDate='Start date is required.';if(!event.endDate)errors.endDate='End date is required.';else if(event.startDate&&event.endDate<event.startDate)errors.endDate='End date cannot be before start date.';if(event.description?.length>1000)errors.description='Description cannot exceed 1000 characters.';return errors}
export function validateClaim(claim,claims=[]){const errors={};if(!claim.societyId)errors.societyId='Select a society.';if(!claim.claimedRoleId)errors.claimedRoleId='Select a role.';if(!claim.startDate)errors.startDate='Start date is required.';else if(claim.startDate>today())errors.startDate='Start date cannot be in the future.';if(!claim.isOngoing&&!claim.endDate)errors.endDate='End date is required when the role is not ongoing.';if(claim.endDate&&claim.startDate&&claim.endDate<claim.startDate)errors.endDate='End date cannot be before start date.';const duplicate=claims.some(other=>other.localId!==claim.localId&&String(other.societyId)===String(claim.societyId)&&String(other.claimedRoleId)===String(claim.claimedRoleId)&&other.isOngoing&&claim.isOngoing);if(duplicate)errors.societyId='An ongoing claim for this society and role already exists.';const eventErrors=(claim.events||[]).map(validateEvent);if(eventErrors.some(x=>Object.keys(x).length))errors.events=eventErrors;return errors}
export const isValidClaim=(claim,claims)=>Object.keys(validateClaim(claim,claims)).length===0
export const claimPayload=claim=>({societyId:claim.societyId,claimedRoleId:claim.claimedRoleId,startDate:claim.startDate,endDate:claim.isOngoing?null:claim.endDate,isOngoing:Boolean(claim.isOngoing),events:(claim.events||[]).map(({eventName,startDate,endDate,description})=>({eventName:eventName.trim(),startDate,endDate,description:description?.trim()||''}))})
