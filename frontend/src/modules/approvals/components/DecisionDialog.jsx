import {useEffect,useState} from 'react'
import {Button,Modal,Textarea} from '../../../design-system'
export default function DecisionDialog({decision,onClose,onConfirm,loading}){const[reason,setReason]=useState(''),[remarks,setRemarks]=useState('');useEffect(()=>{setReason('');setRemarks('')},[decision]);if(!decision)return null;const needsReason=!decision.action.includes('approve');return <Modal open title={decision.title} onClose={onClose} footer={<><Button variant="ghost" onClick={onClose}>Cancel</Button><Button variant={decision.action.includes('reject')?'danger':'primary'} loading={loading} disabled={needsReason&&!reason.trim()} onClick={()=>onConfirm({reason:reason.trim()||undefined,remarks:remarks.trim()||undefined})}>{decision.label}</Button></>}><div className="approval-dialog">{needsReason&&<Textarea label="Reason" required value={reason} onChange={e=>setReason(e.target.value)} placeholder="Explain the decision to the student."/>}<Textarea label="Reviewer remark (optional)" value={remarks} onChange={e=>setRemarks(e.target.value)}/></div></Modal>}
