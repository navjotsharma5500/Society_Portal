import {Card,PageHeader} from '../../../design-system'
export default function StudentPortalPlaceholder({title}){return <><PageHeader eyebrow="Student Portal" title={title}/><Card title={`${title} is coming soon`}><p>This area is reserved for a future portal capability.</p></Card></>}
