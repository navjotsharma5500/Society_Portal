import {useRef,useState} from 'react'
import {useNavigate} from 'react-router-dom'
import {Award,Building2,CalendarDays,ShieldCheck,TriangleAlert,UsersRound} from 'lucide-react'
import {motion} from 'framer-motion'
import {useToast} from '../../../components/common/toastContext'
import TietLogo from '../../../components/branding/TietLogo'
import {isGoogleAuthConfigured} from '../../../config/environment'
import {LOGIN_BACKGROUND_URL} from '../../../config/brandAssets'
import {useAuth} from '../hooks/useAuth'
import {resolveNextActionRoute} from '../utils/authNextAction'
import {AUTH_ERROR_MESSAGES,getAuthErrorMessage} from '../utils/authErrorMessages'
import AuthFeatureCard from '../components/AuthFeatureCard'
import AuthStatusMessage from '../components/AuthStatusMessage'
import GoogleAuthButton from '../components/GoogleAuthButton'
import '../student-auth.css'
export default function StudentLoginPage(){const navigate=useNavigate(),{notify}=useToast(),auth=useAuth(),[activeAction,setActiveAction]=useState(null),[message,setMessage]=useState(''),requestInFlight=useRef(false);const routeResponse=data=>{const route=resolveNextActionRoute(data?.nextAction||data?.onboarding?.nextAction);if(!route){setMessage(AUTH_ERROR_MESSAGES.UNKNOWN_NEXT_ACTION);return}navigate(route,{replace:true})};const authenticate=async(intent,idToken)=>{if(!isGoogleAuthConfigured){setMessage(AUTH_ERROR_MESSAGES.GOOGLE_NOT_CONFIGURED);return}if(requestInFlight.current)return;requestInFlight.current=true;setActiveAction(intent);setMessage('');try{routeResponse(await(intent==='signup'?auth.signUpWithGoogle(idToken):auth.signInWithGoogle(idToken)))}catch(error){const friendly=getAuthErrorMessage(error);setMessage(friendly);notify(friendly,'error')}finally{requestInFlight.current=false;setActiveAction(null)}};const googleError=code=>{const friendly=AUTH_ERROR_MESSAGES[code]||AUTH_ERROR_MESSAGES.GOOGLE_AUTH_FAILED;setMessage(friendly);notify(friendly,'error')};return <main className="student-auth-page"><section className="student-auth-brand" style={{'--login-background-image':`url("${LOGIN_BACKGROUND_URL}")`}} aria-labelledby="auth-brand-title"><div className="auth-grid-pattern"/><div className="auth-glow auth-glow-one"/><div className="auth-glow auth-glow-two"/><div className="auth-brand-content"><div className="auth-institution"><TietLogo size="large" className="auth-brand-logo"/><div><b>Society Portal</b><small>Thapar Institute of Engineering & Technology</small></div></div><span className="auth-campus-label">Campus Connect</span><h1 id="auth-brand-title">Your society journey, connected.</h1><p>Manage participation, memberships, verification, events and certificates from one unified portal.</p><div className="auth-features"><AuthFeatureCard icon={UsersRound} title="Society Participation" description="Discover communities and keep your involvement organized."/><AuthFeatureCard icon={ShieldCheck} title="Verified Memberships" description="Build a trusted record of every role and contribution."/><AuthFeatureCard icon={Award} title="Events & Certificates" description="Follow experiences and preserve campus achievements."/></div><footer><span><Building2 size={14}/>Campus Connect</span><small>Built for the TIET student community</small></footer></div></section><section className="student-auth-panel"><motion.div className="student-auth-card" initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:.24}}><div className="auth-card-logo"><TietLogo size="medium"/><div><b>Society Portal</b><small>Student access</small></div></div><div className="auth-card-heading"><CalendarDays size={20}/><h2>Welcome to Society Portal</h2><p>Use your registered Thapar Google account to continue.</p></div>{!isGoogleAuthConfigured&&<div className="auth-config-warning" role="status"><TriangleAlert size={17}/><div><b>Google Sign-In configuration required</b><p>Add VITE_GOOGLE_CLIENT_ID to frontend/.env and restart the Vite server.</p></div></div>}<AuthStatusMessage message={message}/><div className="auth-action-section"><div><span>First time here?</span><p>Create your portal profile if your details have already been added by the administrator.</p></div><GoogleAuthButton configured={isGoogleAuthConfigured} label="Sign Up with Google" intent="signup" disabled={Boolean(activeAction)} onCredential={token=>authenticate('signup',token)} onError={googleError}/></div><div className="auth-section-divider"><span>or</span></div><div className="auth-action-section returning"><div><span>Already registered?</span><p>Continue your onboarding, check verification status or open your dashboard.</p></div><GoogleAuthButton configured={isGoogleAuthConfigured} label="Sign In with Google" intent="signin" disabled={Boolean(activeAction)} onCredential={token=>authenticate('signin',token)} onError={googleError}/></div><p className="auth-help">Your login will work only when your registered email exists in the Society Portal database.</p><p className="auth-security"><ShieldCheck size={14}/>Secured with Google Identity Services. Your Google password is never shared with the portal.</p></motion.div></section></main>}
